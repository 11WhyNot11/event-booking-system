spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/events
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 0